# 命令简介 

`schedule clean` 主动清除过期的分钟计划

**!!!本命令需要`root`组管理员权限才能执行**

# 用法

```bash
schedule clean
  [-u xxx]            # 仅针对某个用户的任务
  [-today 2021-09-11] # 指定一个日期作为“今天”
  [-limt 1000]        # 是否强制加载。否则，如果已经加载过了就不加载了
  [-remain]           # 返回任务堆栈里剩余任务摘要
```

# 示例

```bash
```