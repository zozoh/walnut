命令简介
======= 

`pay re` 命令将处理支付单平台的回调。

 - 如果支付单填写了回调执行脚本，也会一并执行
 - 如果支付单已经完成，不会重复执行，但是输出同样的输出
    
用法
=======

```    
pay re [poId]     # 「必」支付单 ID
       [JSON]     # 「选」回调发送的请求，如果没有，则从标准输入读取
```

命令输出与 `pay send` 格式相同

示例
=======

```
# 处理某支付单的后续回调
demo@~$ httpparam 5g..9a | pay re 45..e3 
```