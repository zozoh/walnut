#------------------------------------------------
# 只是通过
^/(websocket|gu|www|a|o|u|p|theme|th|pay|_)/(.*)$  => /${1}/${2}
#------------------------------------------------
# httpapi
# 比如 abc.com/api/get 会变成 /api/abc/get
#
^/api/(.*)$           => /api/${grp}/${1}
#------------------------------------------------
# www 模块的映射  
^/(.*)$               => /www/${grp}/${1}  