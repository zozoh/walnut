#------------------------------------------------
# 只是通过
^/(gu|a|o|u|p|theme)/(.*)$  => /${1}/${2}
#------------------------------------------------
# httpapi
^/api/(.*)$           => /api/${grp}/${1}
#------------------------------------------------
# www 模块的映射  
^/(.*)$               => /www/${grp}/${1}  