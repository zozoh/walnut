#------------------------------------------------
# 只是通过
^/(gu|a|o|u|p|theme|th|api)/(.*)$  => /${1}/${2}
#------------------------------------------------
# www 模块的映射  
^/(.*)$               => /www/${grp}/${1}  